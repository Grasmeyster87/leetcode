3464. Maximize the Distance Between Points on a Square
Hard

<p>You are given an integer <code><font face="monospace">side</font></code>, representing the edge length of a square with corners at <code>(0, 0)</code>, <code>(0, side)</code>, <code>(side, 0)</code>, and <code>(side, side)</code> on a Cartesian plane.</p>

<p>You are also given a <strong>positive</strong> integer <code>k</code> and a 2D integer array <code>points</code>, where <code>points[i] = [x<sub>i</sub>, y<sub>i</sub>]</code> represents the coordinate of a point lying on the <strong>boundary</strong> of the square.</p>

<p>You need to select <code>k</code> elements among <code>points</code> such that the <strong>minimum</strong> Manhattan distance between any two points is <strong>maximized</strong>.</p>

<p>Return the <strong>maximum</strong> possible <strong>minimum</strong> Manhattan distance between the selected <code>k</code> points.</p>

<p>The Manhattan Distance between two cells <code>(x<sub>i</sub>, y<sub>i</sub>)</code> and <code>(x<sub>j</sub>, y<sub>j</sub>)</code> is <code>|x<sub>i</sub> - x<sub>j</sub>| + |y<sub>i</sub> - y<sub>j</sub>|</code>.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">side = 2, points = [[0,2],[2,0],[2,2],[0,0]], k = 4</span></p>

<p><strong>Output:</strong> <span class="example-io">2</span></p>

<p><strong>Explanation:</strong></p>

<p><img alt="" style="width: 200px; height: 200px;" src="https://assets.leetcode.com/uploads/2025/01/28/4080_example0_revised.png"></p>

<p>Select all four points.</p>
</div>

<p><strong class="example">Example 2:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">side = 2, points = [[0,0],[1,2],[2,0],[2,2],[2,1]], k = 4</span></p>

<p><strong>Output:</strong> <span class="example-io">1</span></p>

<p><strong>Explanation:</strong></p>

<p><img alt="" style="width: 211px; height: 200px;" src="https://assets.leetcode.com/uploads/2025/01/28/4080_example1_revised.png"></p>

<p>Select the points <code>(0, 0)</code>, <code>(2, 0)</code>, <code>(2, 2)</code>, and <code>(2, 1)</code>.</p>
</div>

<p><strong class="example">Example 3:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">side = 2, points = [[0,0],[0,1],[0,2],[1,2],[2,0],[2,2],[2,1]], k = 5</span></p>

<p><strong>Output:</strong> <span class="example-io">1</span></p>

<p><strong>Explanation:</strong></p>

<p><img alt="" style="width: 200px; height: 200px;" src="https://assets.leetcode.com/uploads/2025/01/28/4080_example2_revised.png"></p>

<p>Select the points <code>(0, 0)</code>, <code>(0, 1)</code>, <code>(0, 2)</code>, <code>(1, 2)</code>, and <code>(2, 2)</code>.</p>
</div>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= side &lt;= 10<sup>9</sup></code></li>
	<li><code>4 &lt;= points.length &lt;= min(4 * side, 15 * 10<sup>3</sup>)</code></li>
	<li><code>points[i] == [xi, yi]</code></li>
	<li>The input is generated such that:
	<ul>
		<li><code>points[i]</code> lies on the boundary of the square.</li>
		<li>All <code>points[i]</code> are <strong>unique</strong>.</li>
	</ul>
	</li>
	<li><code>4 &lt;= k &lt;= min(25, points.length)</code></li>
</ul>

<br> <hr> <br>

3464. Максимизировать расстояние между точками на квадрате
Сложно

<p>Вам дано целое число <code><font face="monospace">side</font></code>, представляющее длину стороны квадрата с углами в точках <code>(0, 0)</code>, <code>(0, side)</code>, <code>(side, 0)</code> и <code>(side, side)</code> на декартовой плоскости.</p>

<p>Вам также дано положительное целое число <code>k</code> и двумерный целочисленный массив <code>points</code>, где <code>points[i] = [x<sub>i</sub>, y<sub>i</sub>]</code> представляет координаты точки, лежащей на <strong>границе</strong> квадрата.</p>

<p>Вам необходимо выбрать <code>k</code> элементов из <code>points</code> таким образом, чтобы <strong>Минимальное</strong> расстояние Манхэттена между любыми двумя точками <strong>максимально</strong>.</p>

<p>Возвращает <strong>максимально</strong> возможное <strong>минимальное</strong> расстояние Манхэттена между выбранными <code>k</code> точками.</p>

<p>Расстояние Манхэттена между двумя ячейками <code>(x<sub>i</sub>, y<sub>i</sub>)</code> и <code>(x<sub>j</sub>, y<sub>j</sub>)</code> равно <code>|x<sub>i</sub> - x<sub>j</sub>| + |y<sub>i</sub> - y<sub>j</sub>|</code>.</p>

<p>&nbsp;</p>
<p><strong class="example">Пример 1:</strong></p>

<div class="example-block">
<p><strong>Ввод:</strong> <span class="example-io">side = 2, points = [[0,2],[2,0],[2,2],[0,0]], k = 4</span></p>

<p><strong>Вывод:</strong> <span class="example-io">2</span></p>

<p><strong>Пояснение:</strong></p>

<p><img alt="" style="width: 200px; height: 200px;" src="https://assets.leetcode.com/uploads/2025/01/28/4080_example0_revised.png"></p>

<p>Выберите все четыре точки.</p>
</div>

<p><strong class="example">Пример 2:</strong></p>

<div class="example-block">
<p><strong>Ввод:</strong> <span class="example-io">side = 2, points = [[0,0],[1,2],[2,0],[2,2],[2,1]], k = 4</span></p>

<p><strong>Вывод:</strong> <span class="example-io">1</span></p>

<p><strong>Пояснение:</strong></p>

<p><img alt="" style="width: 211px; высота: 200px;" src="https://assets.leetcode.com/uploads/2025/01/28/4080_example1_revised.png"></p>

<p>Выберите точки <code>(0, 0)</code>, <code>(2, 0)</code>, <code>(2, 2)</code> и <code>(2, 1)</code>.</p>
</div>

<p><strong class="example">Пример 3:</strong></p>

<div class="example-block">
<p><strong>Ввод:</strong> <span class="example-io">side = 2, points = [[0,0],[0,1],[0,2],[1,2],[2,0],[2,2],[2,1]], k = 5</span></p>

<p><strong>Вывод:</strong> <span class="example-io">1</span></p>

<p><strong>Пояснение:</strong></p>

<p><img alt="" style="width: 200px; height: 200px;" src="https://assets.leetcode.com/uploads/2025/01/28/4080_example2_revised.png"></p>

<p>Выберите точки <code>(0, 0)</code>, <code>(0, 1)</code>, <code>(0, 2)</code>, <code>(1, 2)</code> и <code>(2, 2)</code>.</p>
</div>

<p>&nbsp;</p>
<p><strong>Ограничения:</strong></p>

<ul>

<li><code>1 <= side <= 10<sup>9</sup></code></li>

<li><code>4 <= points.length <= min(4 * side, 15 * 10<sup>3</sup>)</code></li>
<li><code>points[i] == [xi, yi]</code></li>

<li>Входные данные генерируются таким образом, что:

<ul>

<li><code>points[i]</code> лежат на границе квадрата.</li>

<li>Все <code>points[i]</code> являются <strong>уникальными</strong>.</li>

</ul>

</li>

<li><code>4 <= k <= min(25, points.length)</code></li>

</ul>

Ця задача має дуже елегантний математичний трюк, який дозволяє перетворити її з 2D у 1D площину. Оскільки ви часто працюєте з алгоритмами на графах і масивах, цей підхід вам точно сподобається.

### Ключова ідея (Математичне зведення)

1. **Перетворення у 1D:** Оскільки всі точки лежать на межі квадрата, ми можемо «розгорнути» периметр квадрата в пряму лінію довжиною `4 * side`. Кожній точці `(x, y)` можна зіставити одне число — її відстань по периметру від точки `(0, 0)` проти годинникової стрілки.
2. **Манхеттенська відстань проти відстані по периметру:** Оскільки нам потрібно вибрати `k >= 4` точок, середня відстань між сусідніми вибраними точками вздовж периметра не може перевищувати `(4 * side) / 4 = side`. Отже, цільова мінімальна відстань $D$ ніколи не буде більшою за `side`. 
    * Для будь-яких двох точок на одному або сусідніх боках квадрата їхня Манхеттенська відстань **абсолютно дорівнює** відстані по периметру.
    * Для точок на протилежних боках їхня Манхеттенська відстань завжди $\ge side$. А оскільки наш максимум $D \le side$, ця відстань завжди задовольнятиме умову.
    * **Висновок:** Замість Манхеттенської відстані ми можемо просто шукати точки, відстань між якими по периметру становить $\ge D$.

### Алгоритм розв'язання

1. Переводимо всі `points` в одновимірні координати на відрізку `[0, 4 * side)`.
2. Сортуємо ці точки за зростанням.
3. Оскільки периметр замкнений (утворює коло), дублюємо масив точок, додавши до копій `4 * side`. Це дозволить легко імітувати рух по колу.
4. Застосовуємо **Бінарний пошук по відповіді (Binary Search on Answer)** у діапазоні `[1, side]`.
5. Для кожної перевірки (`check(D)`):
   * Використовуємо метод двох вказівників (Two Pointers), щоб швидко для кожної точки знайти найближчу наступну точку на відстані $\ge D$.
   * Проходимо циклом по всіх можливих початкових точках (від `0` до `N-1`) і робимо `k-1` «стрибків». Якщо після `k-1` стрибків відстань між останньою та першою точкою також $\ge D$ (тобто `відстань <= perimeter - D`), тоді відстань $D$ є можливою.

### Рішення на JavaScript

```javascript
/**
 * @param {number} side
 * @param {number[][]} points
 * @param {number} k
 * @return {number}
 */
var maxDistance = function(side, points, k) {
    const n = points.length;
    // Використовуємо Float64Array для оптимізації та уникнення переповнення 
    // (JavaScript Number підтримує точність до 9*10^15, що з запасом покриває 4*10^9)
    const A = new Float64Array(n);
    
    // 1. Перетворення 2D координат у 1D відстань вздовж периметра
    for (let i = 0; i < n; i++) {
        const x = points[i][0];
        const y = points[i][1];
        
        if (y === 0) {
            A[i] = x;                       // Нижній край
        } else if (x === side) {
            A[i] = side + y;                // Правий край
        } else if (y === side) {
            A[i] = 3 * side - x;            // Верхній край
        } else {
            A[i] = 4 * side - y;            // Лівий край
        }
    }
    
    // Float64Array за замовчуванням сортує числа правильно (числове сортування, а не строкове)
    A.sort(); 
    
    const doubleA = new Float64Array(2 * n);
    const perimeter = 4 * side;
    
    // Дублюємо масив для циклічності
    for (let i = 0; i < n; i++) {
        doubleA[i] = A[i];
        doubleA[i + n] = A[i] + perimeter;
    }
    
    const nextIdx = new Int32Array(2 * n);
    
    // Функція для перевірки, чи можливо розставити k точок на відстані >= D
    function check(D) {
        let j = 0;
        // Метод двох вказівників для попереднього обчислення наступних "стрибків"
        for (let i = 0; i < 2 * n; i++) {
            while (j < 2 * n && doubleA[j] - doubleA[i] < D) {
                j++;
            }
            nextIdx[i] = j;
        }
        
        // Шукаємо валідне вікно серед n можливих початкових точок
        for (let i = 0; i < n; i++) {
            let curr = i;
            let valid = true;
            
            // Робимо (k - 1) стрибків
            for (let step = 1; step < k; step++) {
                curr = nextIdx[curr];
                if (curr >= 2 * n) {
                    valid = false;
                    break;
                }
            }
            
            // Перевіряємо відстань "замикання" між останньою і початковою точкою
            if (valid && doubleA[curr] - doubleA[i] <= perimeter - D) {
                return true;
            }
        }
        
        return false;
    }
    
    // Бінарний пошук відповіді
    let low = 1;
    let high = side; 
    let ans = 0;
    
    while (low <= high) {
        let mid = Math.floor((low + high) / 2);
        
        if (check(mid)) {
            ans = mid;
            low = mid + 1; // Пробуємо збільшити дистанцію
        } else {
            high = mid - 1; // Зменшуємо цільову дистанцію
        }
    }
    
    return ans;
};
```

### Складність
* **Часова складність:** $\mathcal{O}(N \log N + N \cdot k \log(\text{side}))$
  * Сортування займає $\mathcal{O}(N \log N)$.
  * Бінарний пошук виконує приблизно $\log_2(10^9) \approx 30$ ітерацій.
  * Усередині кожної ітерації підготовка `nextIdx` за допомогою двох вказівників проходить за $\mathcal{O}(N)$. Симуляція стрибків потребує $\mathcal{O}(N \cdot k)$ операцій. Оскільки $k \le 25$, алгоритм працюватиме неймовірно швидко, легко вписуючись у ліміти виконання.
* **Просторова складність:** $\mathcal{O}(N)$ на виділення типізованих масивів для `A`, `doubleA` та `nextIdx`. Typed Arrays (`Float64Array`, `Int32Array`) також додатково оптимізують швидкодію та кеш пам'яті (замість стандартних масивів JS).