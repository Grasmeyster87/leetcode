3161. Block Placement Queries

Hard
<p>There exists an infinite number line, with its origin at 0 and extending towards the <strong>positive</strong> x-axis.</p>

<p>You are given a 2D array <code>queries</code>, which contains two types of queries:</p>

<ol>
	<li>For a query of type 1, <code>queries[i] = [1, x]</code>. Build an obstacle at distance <code>x</code> from the origin. It is guaranteed that there is <strong>no</strong> obstacle at distance <code>x</code> when the query is asked.</li>
	<li>For a query of type 2, <code>queries[i] = [2, x, sz]</code>. Check if it is possible to place a block of size <code>sz</code> <em>anywhere</em> in the range <code>[0, x]</code> on the line, such that the block <strong>entirely</strong> lies in the range <code>[0, x]</code>. A block <strong>cannot </strong>be placed if it intersects with any obstacle, but it may touch it. Note that you do<strong> not</strong> actually place the block. Queries are separate.</li>
</ol>

<p>Return a boolean array <code>results</code>, where <code>results[i]</code> is <code>true</code> if you can place the block specified in the <code>i<sup>th</sup></code> query of type 2, and <code>false</code> otherwise.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">queries = [[1,2],[2,3,3],[2,3,1],[2,2,2]]</span></p>

<p><strong>Output:</strong> <span class="example-io">[false,true,true]</span></p>

<p><strong>Explanation:</strong></p>

<p><strong><img alt="" style="padding: 10px; background: rgb(255, 255, 255); border-radius: 0.5rem; width: 309px; height: 129px;" src="https://assets.leetcode.com/uploads/2024/04/22/example0block.png"></strong></p>

<p>For query 0, place an obstacle at <code>x = 2</code>. A block of size at most 2 can be placed before <code>x = 3</code>.</p>
</div>

<p><strong class="example">Example 2:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">queries = </span>[[1,7],[2,7,6],[1,2],[2,7,5],[2,7,6]]</p>

<p><strong>Output:</strong> [true,true,false]</p>

<p><strong>Explanation:</strong></p>

<p><strong><img alt="" style="padding: 10px; background: rgb(255, 255, 255); border-radius: 0.5rem; width: 310px; height: 130px;" src="https://assets.leetcode.com/uploads/2024/04/22/example1block.png"></strong></p>

<ul>
	<li>Place an obstacle at <code>x = 7</code> for query 0. A block of size at most 7 can be placed before <code>x = 7</code>.</li>
	<li>Place an obstacle at <code>x = 2</code> for query 2. Now, a block of size at most 5 can be placed before <code>x = 7</code>, and a block of size at most 2 before <code>x = 2</code>.</li>
</ul>
</div>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= queries.length &lt;= 15 * 10<sup>4</sup></code></li>
	<li><code>2 &lt;= queries[i].length &lt;= 3</code></li>
	<li><code>1 &lt;= queries[i][0] &lt;= 2</code></li>
	<li><code>1 &lt;= x, sz &lt;= min(5 * 10<sup>4</sup>, 3 * queries.length)</code></li>
	<li>The input is generated such that for queries of type 1, no obstacle exists at distance <code>x</code> when the query is asked.</li>
	<li>The input is generated such that there is at least one query of type 2.</li>
</ul>

<br> <hr> <br>

3161. Запросы на размещение блоков

Сложно
<p>Существует бесконечная числовая прямая, начало которой находится в 0, и которая простирается в направлении положительной оси x.</p>

<p>Вам дан двумерный массив <code>queries</code>, содержащий два типа запросов:</p>

<ol>

<li>Для запроса типа 1, <code>queries[i] = [1, x]</code>. Постройте препятствие на расстоянии <code>x</code> от начала координат. Гарантируется, что на расстоянии <strong>x</code> нет препятствий в момент выполнения запроса.</li>

<li>Для запроса типа 2, <code>queries[i] = [2, x, sz]</code>. Проверьте, возможно ли разместить блок размером <code>sz</code> в любом месте <code>[0, x]</code> на линии так, чтобы блок <strong>полностью</strong> находился в диапазоне <code>[0, x]</code>. Блок <strong>не может быть размещен, если он пересекается с каким-либо препятствием, но может касаться его. Обратите внимание, что вы <strong>фактически</strong> не размещаете блок. Запросы разделены.</li>
</ol>

<p>Возвращает логический массив <code>results</code>, где <code>results[i]</code> равно <code>true</code>, если вы можете разместить указанный блок в <code>i<sup>th</sup></code> запросе типа 2, и <code>false</code> в противном случае.</p>

<p>&nbsp;</p>
<p><strong class="example">Пример 1:</strong></p>

<div class="example-block">
<p><strong>Входные данные:</strong> <span class="example-io">queries = [[1,2],[2,3,3],[2,3,1],[2,2,2]]</span></p>

<p><strong>Выходные данные:</strong> <span class="example-io">[false,true,true]</span></p>

<p><strong>Пояснение:</strong></p>

<p><strong><img alt="" style="padding: 10px; background: rgb(255, 255, 255); border-radius: 0.5rem; width: 309px; height: 129px;" src="https://assets.leetcode.com/uploads/2024/04/22/example0block.png"></strong></p>

<p>Для запроса 0 разместите препятствие в точке <code>x = 2</code>. Перед <code>x = 3</code> можно разместить блок размером не более 2.

</div>

<p><strong class="example">Пример 2:</strong></p>

<div class="example-block">
<p><strong>Ввод:</strong> <span class="example-io">queries = </span>[[1,7],[2,7,6],[1,2],[2,7,5],[2,7,6]]</p>

<p><strong>Вывод:</strong> [true,true,false]</p>

<p><strong>Пояснение:</strong></p>

<p><strong><img alt="" style="padding: 10px; background: rgb(255, 255, 255); border-radius: 0.5rem; width: 310px; height: 130px;" src="https://assets.leetcode.com/uploads/2024/04/22/example1block.png"></strong></p>

<ul>

<li>Разместите препятствие в точке <code>x = 7</code> для запроса 0. Блок размером не более 7 может быть размещен перед <code>x = 7</code>.</li>

<li>Разместите препятствие в точке <code>x = 2</code> для запроса 2. Теперь блок размером не более 5 может быть размещен перед <code>x = 7</code>, а блок размером не более 2 — перед <code>x = 2</code>.</li>
</ul>
</div>

<p>&nbsp;</p>
<p><strong>Ограничения:</strong></p>

<ul>

<li><code>1 <= queries.length <= 15 * 10<sup>4</sup></code></li>
<li><code>2 <= queries[i].length <= 3</code></li>

<li><code>1 <= queries[i][0] <= 2</code></li>

<li><code>1 <= x, sz <= min(5 * 10<sup>4</sup>, 3 * queries.length)</code></li>

<li>Входные данные генерируются таким образом, что для запросов типа 1 на расстоянии <code>x</code> не существует препятствий.</li>

<li>Входные данные генерируются таким образом, что существует как минимум один запрос типа 2.</li>
</ul>

Привіт! Я успішно реалізував рішення для задачі 3161. Block Placement Queries у файлі 3161. Block Placement Queries.js.

Пояснення підходу (Segment Tree)
Щоб ефективно опрацьовувати запити, я використав Дерево відрізків (Segment Tree). Оскільки максимальне значення координати $x$ за умовою не перевищує 50 000 (або пропорційно невелике в залежності від кількості запитів), ми можемо побудувати дерево відрізків по всій множині можливих координат.

Для кожного вузла (відрізка) в дереві зберігається:

max_gap: максимальна відстань між двома сусідніми перешкодами, що знаходяться повністю всередині цього відрізка.
max_obs: координата найвіддаленішої перешкоди (максимальний $x$) на цьому відрізку.
min_obs: координата найближчої перешкоди (мінімальний $x$) на цьому відрізку.
Як обробляються запити:

Запит 1 (Додати перешкоду на $x$): Ми спочатку знаходимо за допомогою дерева попередню перешкоду prev перед $x$ і наступну next після $x$. Після цього додаємо перешкоду $x$, встановлюючи для неї розрив до попередньої перешкоди ($x - prev$), а також оновлюємо розрив для next, адже тепер він дорівнює $next - x$.
Запит 2 (Перевірити можливість розміщення блоку розміру $sz$ до $x$): Ми знаходимо останню перешкоду prev на проміжку $[0, x]$. Максимальний блок, який можна розмістити, дорівнюватиме максимуму з двох значень:
Максимального розриву між перешкодами, що повністю лежать зліва від prev (його беремо з max_gap).
Залишкової відстані від prev до кінцевої точки $x$ ($x - prev$).
Кожен запит виконується за $O(\log M)$, де $M$ — максимальне значення $x$ серед усіх запитів. Такий підхід працює дуже швидко й гарантовано вкладається в ліміти часу для $1.5 \cdot 10^5$ запитів. Пам'ять також оптимізована за рахунок використання швидких типізованих масивів Int32Array.

Ви можете переглянути готовий код у файлі 

3161. Block Placement Queries.js
. Якщо у вас виникнуть додаткові питання або потрібно буде вирішити інші задачі — звертайтесь!