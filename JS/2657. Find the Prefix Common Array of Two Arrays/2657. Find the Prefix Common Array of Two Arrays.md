2657. Find the Prefix Common Array of Two Arrays

Medium

<p>You are given two <strong>0-indexed </strong>integer<strong> </strong>permutations <code>A</code> and <code>B</code> of length <code>n</code>.</p>

<p>A <strong>prefix common array</strong> of <code>A</code> and <code>B</code> is an array <code>C</code> such that <code>C[i]</code> is equal to the count of numbers that are present at or before the index <code>i</code> in both <code>A</code> and <code>B</code>.</p>

<p>Return <em>the <strong>prefix common array</strong> of </em><code>A</code><em> and </em><code>B</code>.</p>

<p>A sequence of <code>n</code> integers is called a&nbsp;<strong>permutation</strong> if it contains all integers from <code>1</code> to <code>n</code> exactly once.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<pre><strong>Input:</strong> A = [1,3,2,4], B = [3,1,2,4]
<strong>Output:</strong> [0,2,3,4]
<strong>Explanation:</strong> At i = 0: no number is common, so C[0] = 0.
At i = 1: 1 and 3 are common in A and B, so C[1] = 2.
At i = 2: 1, 2, and 3 are common in A and B, so C[2] = 3.
At i = 3: 1, 2, 3, and 4 are common in A and B, so C[3] = 4.
</pre>

<p><strong class="example">Example 2:</strong></p>

<pre><strong>Input:</strong> A = [2,3,1], B = [3,1,2]
<strong>Output:</strong> [0,1,3]
<strong>Explanation:</strong> At i = 0: no number is common, so C[0] = 0.
At i = 1: only 3 is common in A and B, so C[1] = 1.
At i = 2: 1, 2, and 3 are common in A and B, so C[2] = 3.
</pre>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= A.length == B.length == n &lt;= 50</code></li>
	<li><code>1 &lt;= A[i], B[i] &lt;= n</code></li>
	<li><code>It is guaranteed that A and B are both a permutation of n integers.</code></li>
</ul>
</div>

<br> <hr> <br>

2657. Найдите префиксный общий массив двух массивов

Средний уровень сложности

<p>Вам даны две <strong>целочисленные перестановки с индексом 0</strong> <code>A</code> и <code>B</code> длиной <code>n</code>.</p>

<p>Префиксный общий массив</strong> массивов <code>A</code> и <code>B</code> — это массив <code>C</code> такой, что <code>C[i]</code> равно количеству чисел, присутствующих на индексе <code>i</code> или перед ним в обоих массивах <code>A</code> и <code>B</code>.</p>

<p>Верните <em>префиксный общий массив</strong> массивов </em><code>A</code><em> и </em><code>B</code>.</p>

<p>Последовательность из <code>n</code> Целые числа называются перестановкой, если они содержат все целые числа от 1 до n ровно один раз.



Пример 1:

Входные данные: A = [1, 3, 2, 4], B = [3, 1, 2, 4]
Выходные данные: [0, 2, 3, 4]
Пояснение: При i = 0: ни одно число не является общим, поэтому C[0] = 0.
При i = 1: 1 и 3 являются общими числами в A и B, поэтому C[1] = 2.
При i = 2: 1, 2 и 3 являются общими числами в A и B, поэтому C[2] = 3.
При i = 3: 1, 2, Числа 3 и 4 являются общими для A и B, поэтому C[3] = 4.
</pre>

<p><strong class="example">Пример 2:</strong></p>

<pre><strong>Входные данные:</strong> A = [2,3,1], B = [3,1,2]
<strong>Выходные данные:</strong> [0,1,3]
<strong>Пояснение:</strong> При i = 0: ни одно число не является общим, поэтому C[0] = 0.
При i = 1: только 3 является общим для A и B, поэтому C[1] = 1.
При i = 2: 1, 2 и 3 являются общими для A и B, поэтому C[2] = 3.
</pre>

<p>&nbsp;</p>
<p><strong>Ограничения:</strong></p>

<ul>

<li><code>1 <= A.length == B.length == n <= 50</code></li>

<li><code>1 <= A[i], B[i] <= n</code></li>

<li><code>Гарантируется, что A и B являются перестановкой n целых чисел.</code></li>
</ul>
</div>

<br> <hr> <br>

## Пояснення рішення

### Суть задачі

Дано два масиви `A` і `B` однакової довжини `n`, кожен — перестановка чисел від `1` до `n`.

`C[i]` = **кількість спільних чисел**, які вже зустрілися в обох масивах від початку до позиції `i` включно.

---

### Ідея алгоритму

Створюємо масив `count` розміром `n + 1` (бо числа від **1** до **n**, а індекси масиву починаються з **0**, тому потрібен індекс `n`).

- Коли число з'являється вперше (з `A` або `B`) → `count` стає **1**.
- Коли число з'являється вдруге (з іншого масиву) → `count` стає **2** → це означає що число тепер **спільне** → збільшуємо `commonCount`.

---

### Покроковий розбір

`A = [1, 3, 2, 4]`, `B = [3, 1, 2, 4]`

`count = [0, 0, 0, 0, 0]`, `commonCount = 0`

**i = 0:** `A[0] = 1`, `B[0] = 3`

- `count[1]++` → `[0, 1, 0, 0, 0]` — число 1 зустрілося 1 раз
- `count[1] === 2`? → Ні
- `count[3]++` → `[0, 1, 0, 1, 0]` — число 3 зустрілося 1 раз
- `count[3] === 2`? → Ні
- **C[0] = 0**

**i = 1:** `A[1] = 3`, `B[1] = 1`

- `count[3]++` → `[0, 1, 0, 2, 0]` — число 3 зустрілося **2** рази!
- `count[3] === 2`? → **Так!** → `commonCount = 1`
- `count[1]++` → `[0, 2, 0, 2, 0]` — число 1 зустрілося **2** рази!
- `count[1] === 2`? → **Так!** → `commonCount = 2`
- **C[1] = 2**

**i = 2:** `A[2] = 2`, `B[2] = 2`

- `count[2]++` → `[0, 2, 1, 2, 0]` — число 2 зустрілося 1 раз
- `count[2] === 2`? → Ні
- `count[2]++` → `[0, 2, 2, 2, 0]` — число 2 зустрілося **2** рази!
- `count[2] === 2`? → **Так!** → `commonCount = 3`
- **C[2] = 3**

**i = 3:** `A[3] = 4`, `B[3] = 4`

- `count[4]++` → `[0, 2, 2, 2, 1]` — число 4 зустрілося 1 раз
- `count[4] === 2`? → Ні
- `count[4]++` → `[0, 2, 2, 2, 2]` — число 4 зустрілося **2** рази!
- `count[4] === 2`? → **Так!** → `commonCount = 4`
- **C[3] = 4**

---

### Результат: `C = [0, 2, 3, 4]` ✅

**Складність**: O(n) за часом і O(n) за пам'яттю.