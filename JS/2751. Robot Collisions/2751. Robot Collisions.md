2751. Robot Collisions
<br><p>Hard</p>
<p>There are <code>n</code> <strong>1-indexed</strong> robots, each having a position on a line, health, and movement direction.</p>

<p>You are given <strong>0-indexed</strong> integer arrays <code>positions</code>, <code>healths</code>, and a string <code>directions</code> (<code>directions[i]</code> is either <strong>'L'</strong> for <strong>left</strong> or <strong>'R'</strong> for <strong>right</strong>). All integers in <code>positions</code> are <strong>unique</strong>.</p>

<p>All robots start moving on the line<strong> simultaneously</strong> at the <strong>same speed </strong>in their given directions. If two robots ever share the same position while moving, they will <strong>collide</strong>.</p>

<p>If two robots collide, the robot with <strong>lower health</strong> is <strong>removed</strong> from the line, and the health of the other robot <strong>decreases</strong> <strong>by one</strong>. The surviving robot continues in the <strong>same</strong> direction it was going. If both robots have the <strong>same</strong> health, they are both<strong> </strong>removed from the line.</p>

<p>Your task is to determine the <strong>health</strong> of the robots that survive the collisions, in the same <strong>order </strong>that the robots were given,<strong> </strong>i.e. final health of robot 1 (if survived), final health of robot 2 (if survived), and so on. If there are no survivors, return an empty array.</p>

<p>Return <em>an array containing the health of the remaining robots (in the order they were given in the input), after no further collisions can occur.</em></p>

<p><strong>Note:</strong> The positions may be unsorted.</p>

<div class="notranslate" style="all: initial;">&nbsp;</div>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<p><img height="169" width="808" src="https://assets.leetcode.com/uploads/2023/05/15/image-20230516011718-12.png"></p>

<pre><strong>Input:</strong> positions = [5,4,3,2,1], healths = [2,17,9,15,10], directions = "RRRRR"
<strong>Output:</strong> [2,17,9,15,10]
<strong>Explanation:</strong> No collision occurs in this example, since all robots are moving in the same direction. So, the health of the robots in order from the first robot is returned, [2, 17, 9, 15, 10].
</pre>

<p><strong class="example">Example 2:</strong></p>

<p><img height="176" width="717" src="https://assets.leetcode.com/uploads/2023/05/15/image-20230516004433-7.png"></p>

<pre><strong>Input:</strong> positions = [3,5,2,6], healths = [10,10,15,12], directions = "RLRL"
<strong>Output:</strong> [14]
<strong>Explanation:</strong> There are 2 collisions in this example. Firstly, robot 1 and robot 2 will collide, and since both have the same health, they will be removed from the line. Next, robot 3 and robot 4 will collide and since robot 4's health is smaller, it gets removed, and robot 3's health becomes 15 - 1 = 14. Only robot 3 remains, so we return [14].
</pre>

<p><strong class="example">Example 3:</strong></p>

<p><img height="172" width="732" src="https://assets.leetcode.com/uploads/2023/05/15/image-20230516005114-9.png"></p>

<pre><strong>Input:</strong> positions = [1,2,5,6], healths = [10,10,11,11], directions = "RLRL"
<strong>Output:</strong> []
<strong>Explanation:</strong> Robot 1 and robot 2 will collide and since both have the same health, they are both removed. Robot 3 and 4 will collide and since both have the same health, they are both removed. So, we return an empty array, [].</pre>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= positions.length == healths.length == directions.length == n &lt;= 10<sup>5</sup></code></li>
	<li><code>1 &lt;= positions[i], healths[i] &lt;= 10<sup>9</sup></code></li>
	<li><code>directions[i] == 'L'</code> or <code>directions[i] == 'R'</code></li>
	<li>All values in <code>positions</code> are distinct</li>
</ul>
</div>

<br> <hr> <br>

2751. Столкновения роботов

<br>Сложно
<p>Имеется <code>n</code> <strong>1-индексированных</strong> роботов, каждый из которых имеет позицию на линии, здоровье и направление движения.</p>

<p>Вам даны <strong>0-индексированные</strong> целочисленные массивы <code>positions</code>, <code>healths</code> и строка <code>directions</code> (<code>directions[i]</code> может быть либо <strong>'L'</strong> для <strong>left</strong>, либо <strong>'R'</strong> для <strong>right</strong>). Все целые числа в <code>positions</code> являются <strong>уникальными</strong>.</p>

<p>Все роботы начинают движение по линии<strong>одновременно</strong> с <strong>одинаковой скоростью</strong> в заданных направлениях. Если два робота окажутся в одной и той же позиции во время движения, они столкнутся.

Если два робота столкнутся, робот с меньшим запасом здоровья удаляется с линии, а здоровье другого робота уменьшается на единицу. Выживший робот продолжает движение в том же направлении. Если у обоих роботов одинаковое здоровье, они оба удаляются с линии.

Ваша задача — определить здоровье роботов, выживших после столкновения, в том же порядке, в котором они были заданы, т.е. конечное здоровье робота 1 (если он выжил), конечное здоровье робота 2 (если он выжил) и так далее. Если выживших нет, вернуть пустой массив.</p>

<p>Возвращает массив, содержащий показатели здоровья оставшихся роботов (в порядке, указанном во входных данных), после того, как дальнейшие столкновения станут невозможны.</em></p>

<p><strong>Примечание:</strong> Позиции могут быть не отсортированы.</p>

<div class="notranslate" style="all: initial;">&nbsp;</div>

<p>&nbsp;</p>
<p><strong class="example">Пример 1:</strong></p>

<p><img height="169" width="808" src="https://assets.leetcode.com/uploads/2023/05/15/image-20230516011718-12.png"></p>

<pre><strong>Входные данные:</strong> positions = [5,4,3,2,1], здоровье = [2,17,9,15,10], направления = "RRRRRR"
<strong>Вывод:</strong> [2,17,9,15,10]
<strong>Пояснение:</strong> В этом примере столкновения не происходит, так как все роботы движутся в одном направлении. Таким образом, возвращается здоровье роботов в порядке убывания, начиная с первого робота: [2, 17, 9, 15, 10].

</pre>

<p><strong class="example">Пример 2:</strong></p>

<p><img height="176" width="717" src="https://assets.leetcode.com/uploads/2023/05/15/image-20230516004433-7.png"></p>

<pre><strong>Входные данные:</strong> positions = [3,5,2,6], healths = [10,10,15,12], directions = "RLRL"
<strong>Выходные данные:</strong> [14]
<strong>Пояснение:</strong> В этом примере происходит 2 столкновения. Во-первых, робот 1 и робот 2 столкнутся, и поскольку у обоих одинаковое количество здоровья, они будут удалены с линии. Далее роботы 3 и 4 столкнутся, и поскольку здоровье робота 4 меньше, он удаляется, а здоровье робота 3 становится 15 - 1 = 14. Остается только робот 3, поэтому мы возвращаемся [14].

</pre>

<p><strong class="example">Пример 3:</strong></p>

<p><img height="172" width="732" src="https://assets.leetcode.com/uploads/2023/05/15/image-20230516005114-9.png"></p>

<pre><strong>Входные данные:</strong> positions = [1,2,5,6], healths = [10,10,11,11], directions = "RLRL"
<strong>Выходные данные:</strong> []
<strong>Пояснение:</strong> Робот 1 и робот 2 столкнутся, и поскольку у обоих одинаковое количество здоровья, они оба будут удалены. Робот 3 и 4 столкнутся, и поскольку у обоих одинаковое количество здоровья, они оба будут удалены. Таким образом, мы возвращаем пустой массив [].</pre>

<p>&nbsp;</p>
<p><strong>Ограничения:</strong></p>

<ul>

<li><code>1 <= positions.length == healths.length == directions.length == n <= 10<sup>5</sup></code></li>
<li><code>1 <= positions[i], healths[i] <= 10<sup>9</sup></code></li>

<li><code>directions[i] == 'L'</code> или <code>directions[i] == 'R'</code></li>

<li>Все значения в <code>positions</code> уникальны</li>
</ul>
</div>