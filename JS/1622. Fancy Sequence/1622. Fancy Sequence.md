1622. Fancy Sequence
Hard
Topics
premium lock iconCompanies
Hint

Write an API that generates fancy sequences using the append, addAll, and multAll operations.

Implement the Fancy class:

    Fancy() Initializes the object with an empty sequence.
    void append(val) Appends an integer val to the end of the sequence.
    void addAll(inc) Increments all existing values in the sequence by an integer inc.
    void multAll(m) Multiplies all existing values in the sequence by an integer m.
    int getIndex(idx) Gets the current value at index idx (0-indexed) of the sequence modulo 109 + 7. If the index is greater or equal than the length of the sequence, return -1.

 

Example 1:

Input
["Fancy", "append", "addAll", "append", "multAll", "getIndex", "addAll", "append", "multAll", "getIndex", "getIndex", "getIndex"]
[[], [2], [3], [7], [2], [0], [3], [10], [2], [0], [1], [2]]
Output
[null, null, null, null, null, 10, null, null, null, 26, 34, 20]

Explanation
Fancy fancy = new Fancy();
fancy.append(2);   // fancy sequence: [2]
fancy.addAll(3);   // fancy sequence: [2+3] -> [5]
fancy.append(7);   // fancy sequence: [5, 7]
fancy.multAll(2);  // fancy sequence: [5*2, 7*2] -> [10, 14]
fancy.getIndex(0); // return 10
fancy.addAll(3);   // fancy sequence: [10+3, 14+3] -> [13, 17]
fancy.append(10);  // fancy sequence: [13, 17, 10]
fancy.multAll(2);  // fancy sequence: [13*2, 17*2, 10*2] -> [26, 34, 20]
fancy.getIndex(0); // return 26
fancy.getIndex(1); // return 34
fancy.getIndex(2); // return 20

 

Constraints:

    1 <= val, inc, m <= 100
    0 <= idx <= 105
    At most 105 calls total will be made to append, addAll, multAll, and getIndex.

<br> <hr> <br>

1622. Fancy Sequence
Сложный
Темы
иконка премиум-блокировки Компании
Подсказка

Напишите API, который генерирует сложные последовательности с помощью операций append, addAll и multAll.

Реализуйте класс Fancy:

Fancy() Инициализирует объект пустой последовательностью.
void append(val) Добавляет целое число val в конец последовательности.
void addAll(inc) Увеличивает все существующие значения в последовательности на целое число inc.
void multAll(m) Умножает все существующие значения в последовательности на целое число m.
int getIndex(idx) Получает текущее значение по индексу idx (с индексом 0) последовательности по модулю 109 + 7. Если индекс больше или равен длине последовательности, возвращает -1.

Пример 1:

Входные данные
["Fancy", "append", "addAll", "append", "multAll", "getIndex", "addAll", "append", "multAll", "getIndex", "getIndex", "getIndex"]
[[], [2], [3], [7], [2], [0], [3], [10], [2], [0], [1], [2]]
Выходные данные
[null, null, null, null, null, 10, null, null, null, 26, 34, 20]

Пояснение
Fancy fancy = new Fancy();
fancy.append(2); // последовательность fancy: [2]
fancy.addAll(3); // последовательность fancy: [2+3] -> [5]
fancy.append(7); // последовательность fancy: [5, 7]
fancy.multAll(2); // fancy sequence: [5*2, 7*2] -> [10, 14]
fancy.getIndex(0); // возвращает 10
fancy.addAll(3); // fancy sequence: [10+3, 14+3] -> [13, 17]
fancy.append(10); // fancy sequence: [13, 17, 10]
fancy.multAll(2); // fancy sequence: [13*2, 17*2, 10*2] -> [26, 34, 20]
fancy.getIndex(0); // возвращает 26
fancy.getIndex(1); // возвращает 34
fancy.getIndex(2); // возвращает 20

Ограничения:

1 <= val, inc, m <= 100
0 <= idx <= 105
Всего будет выполнено не более 105 вызовов функций append, addAll, multAll и getIndex.

Ця задача вимагає ефективного керування операціями над усією послідовністю. Пряме оновлення кожного елемента при кожному виклику `addAll` або `multAll` призведе до перевищення ліміту часу ($O(N^2)$). Оптимальний підхід — використання математики лінійних перетворень.

### Логіка рішення

Будь-яку послідовність операцій додавання та множення можна представити у вигляді лінійної функції: $f(x) = (a \cdot x + b) \pmod M$, де:

* $a$ — кумулятивний множник.
* $b$ — кумулятивний доданок.
* $M = 10^9 + 7$.

При додаванні елемента `val` ми зберігаємо його не "як є", а у "зворотному" вигляді, враховуючи поточні значення $a$ та $b$. Це дозволяє пізніше застосувати до нього актуальні глобальні коефіцієнти одним махом.

1. **`append(val)`**: Ми шукаємо таке $x$, щоб $a \cdot x + b \equiv val \pmod M$. Отже, $x = (val - b) \cdot a^{-1} \pmod M$.
2. **`addAll(inc)`**: Оновлюємо лише зміщення: $b = (b + inc) \pmod M$.
3. **`multAll(m)`**: Оновлюємо обидва коефіцієнти: $a = (a \cdot m) \pmod M$ та $b = (b \cdot m) \pmod M$.
4. **`getIndex(idx)`**: Результат — це $(a \cdot x_{idx} + b) \pmod M$.

Для обчислення $a^{-1}$ (модульного зворотного елемента) використаємо **Малу теорему Ферма**, оскільки $10^9 + 7$ — просте число: $a^{M-2} \pmod M$.

---

### Реалізація (JavaScript)

```javascript
const MOD = BigInt(1e9 + 7);

var Fancy = function() {
    this.list = [];
    this.a = 1n; // Кумулятивний множник
    this.b = 0n; // Кумулятивний доданок
};

/** * Допоміжна функція для швидкого піднесення до степеня за модулем
 */
Fancy.prototype.power = function(base, exp) {
    let res = 1n;
    base %= MOD;
    while (exp > 0n) {
        if (exp % 2n === 1n) res = (res * base) % MOD;
        base = (base * base) % MOD;
        exp /= 2n;
    }
    return res;
};

/** * Обчислення модульного зворотного елемента (a^-1 mod M)
 */
Fancy.prototype.modInverse = function(n) {
    return this.power(n, MOD - 2n);
};

/** * @param {number} val
 * @return {void}
 */
Fancy.prototype.append = function(val) {
    // Зберігаємо значення x, яке при поточному a та b дасть val
    // x = (val - b) / a  => (val - b) * modInverse(a)
    let valBI = BigInt(val);
    let x = ((valBI - this.b + MOD) % MOD * this.modInverse(this.a)) % MOD;
    this.list.push(x);
};

/** * @param {number} inc
 * @return {void}
 */
Fancy.prototype.addAll = function(inc) {
    this.b = (this.b + BigInt(inc)) % MOD;
};

/** * @param {number} m
 * @return {void}
 */
Fancy.prototype.multAll = function(m) {
    let mBI = BigInt(m);
    this.a = (this.a * mBI) % MOD;
    this.b = (this.b * mBI) % MOD;
};

/** * @param {number} idx
 * @return {number}
 */
Fancy.prototype.getIndex = function(idx) {
    if (idx >= this.list.length) return -1;
    // Обчислюємо поточне значення: (a * x + b) % MOD
    return Number((this.a * this.list[idx] + this.b) % MOD);
};

```

### Чому це працює швидко:

* **`append`**, **`addAll`**, **`multAll`**: Працюють за $O(1)$ (якщо не враховувати час на `modInverse`, який займає $O(\log MOD)$).
* **`getIndex`**: Працює за $O(1)$.
* Загальна складність для $N$ операцій складе приблизно $O(N \log MOD)$, що цілком вкладається в ліміти часу для $10^5$ викликів.

Чи хочете ви, щоб я допоміг перенести цей алгоритм на **C++** або **Python**, враховуючи ваші попередні роботи з цими мовами?