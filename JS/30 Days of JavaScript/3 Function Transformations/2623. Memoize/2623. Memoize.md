2623. Memoize

Given a function fn, return a memoized version of that function.

A memoized function is a function that will never be called twice with the same inputs. Instead it will return a cached value.

You can assume there are 3 possible input functions: sum, fib, and factorial.

    sum accepts two integers a and b and returns a + b. Assume that if a value has already been cached for the arguments (b, a) where a != b, it cannot be used for the arguments (a, b). For example, if the arguments are (3, 2) and (2, 3), two separate calls should be made.
    fib accepts a single integer n and returns 1 if n <= 1 or fib(n - 1) + fib(n - 2) otherwise.
    factorial accepts a single integer n and returns 1 if n <= 1 or factorial(n - 1) * n otherwise.

 

Example 1:

Input:
fnName = "sum"
actions = ["call","call","getCallCount","call","getCallCount"]
values = [[2,2],[2,2],[],[1,2],[]]
Output: [4,4,1,3,2]
Explanation:
const sum = (a, b) => a + b;
const memoizedSum = memoize(sum);
memoizedSum(2, 2); // "call" - returns 4. sum() was called as (2, 2) was not seen before.
memoizedSum(2, 2); // "call" - returns 4. However sum() was not called because the same inputs were seen before.
// "getCallCount" - total call count: 1
memoizedSum(1, 2); // "call" - returns 3. sum() was called as (1, 2) was not seen before.
// "getCallCount" - total call count: 2

Example 2:

Input:
fnName = "factorial"
actions = ["call","call","call","getCallCount","call","getCallCount"]
values = [[2],[3],[2],[],[3],[]]
Output: [2,6,2,2,6,2]
Explanation:
const factorial = (n) => (n <= 1) ? 1 : (n * factorial(n - 1));
const memoFactorial = memoize(factorial);
memoFactorial(2); // "call" - returns 2.
memoFactorial(3); // "call" - returns 6.
memoFactorial(2); // "call" - returns 2. However factorial was not called because 2 was seen before.
// "getCallCount" - total call count: 2
memoFactorial(3); // "call" - returns 6. However factorial was not called because 3 was seen before.
// "getCallCount" - total call count: 2

Example 3:

Input:
fnName = "fib"
actions = ["call","getCallCount"]
values = [[5],[]]
Output: [8,1]
Explanation:
fib(5) = 8 // "call"
// "getCallCount" - total call count: 1

 

Constraints:

    0 <= a, b <= 105
    1 <= n <= 10
    1 <= actions.length <= 105
    actions.length === values.length
    actions[i] is one of "call" and "getCallCount"
    fnName is one of "sum", "factorial" and "fib"

<br> <hr> <br>

2623. Мемоизация

Дана функция fn, верните мемоизированную версию этой функции.

Мемоизированная функция — это функция, которая никогда не будет вызываться дважды с одними и теми же входными данными. Вместо этого она будет возвращать кэшированное значение.

Предположим, что существует 3 возможных входных функции: сумма, фиброз и факториал.

Функция sum принимает два целых числа a и b и возвращает a + b. Предположим, что если значение уже было кэшировано для аргументов (b, a), где a != b, оно не может быть использовано для аргументов (a, b). Например, если аргументы (3, 2) и (2, 3), следует сделать два отдельных вызова.
Функция fib принимает одно целое число n и возвращает 1, если n <= 1, или fib(n - 1) + fib(n - 2) в противном случае.

Функция fib принимает одно целое число n и возвращает 1, если n <= 1, или fib(n - 1) + fib(n - 2) в противном случае.

Функция factorial принимает одно целое число n и возвращает 1, если n <= 1, или factorial(n - 1) * n в противном случае.

Пример 1:

Входные данные:
fnName = "sum"
actions = ["call","call","getCallCount","call","getCallCount"]
values ​​= [[2,2],[2,2],[],[1,2],[]]
Выходные данные: [4,4,1,3,2]
Пояснение:
const sum = (a, b) => a + b;
const memoizedSum = memoize(sum);
memoizedSum(2, 2); // "call" - возвращает 4. Функция sum() была вызвана, так как (2, 2) ранее не встречалась.

memoizedSum(2, 2); // "call" - возвращает 4. Однако функция sum() не была вызвана, так как те же входные данные встречались ранее.

// "getCallCount" - общее количество звонков: 1
memoizedSum(1, 2); // "call" - возвращает 3. Функция sum() была вызвана, так как (1, 2) ранее не встречалась.

// "getCallCount" - общее количество звонков: 2

Пример 2:

Входные данные:
fnName = "factorial"
actions = ["call","call","call","getCallCount","call","getCallCount"]
values ​​= [[2],[3],[2],[],[3],[]]
Выходные данные: [2,6,2,2,6,2]
Пояснение:
const factorial = (n) => (n <= 1) ? 1 : (n * factorial(n - 1));
const memoFactorial = memoize(factorial);
memoFactorial(2); // "call" - возвращает 2.
memoFactorial(3); // "call" - возвращает 6.
memoFactorial(2); // "call" - возвращает 2. Однако функция factorial не была вызвана, потому что ранее было обнаружено число 2.

// "getCallCount" - общее количество вызовов: 2
memoFactorial(3); // "call" - возвращает 6. Однако функция factorial не была вызвана, потому что ранее было обнаружено число 3.

// "getCallCount" - общее количество звонков: 2

Пример 3:

Входные данные:
fnName = "fib"
actions = ["call","getCallCount"]
values ​​= [[5],[]]
Выходные данные: [8,1]
Пояснение:
fib(5) = 8 // "call"
// "getCallCount" - общее количество звонков: 1

Ограничения:

0 <= a, b <= 105
1 <= n <= 10

1 <= actions.length <= 105
actions.length === values.length
actions[i] - одно из значений "call" и "getCallCount"
fnName - одно из значений "sum", "factorial" и "fib"