2723. Add Two Promises

Given two promises promise1 and promise2, return a new promise. promise1 and promise2 will both resolve with a number. The returned promise should resolve with the sum of the two numbers.

Example 1:

Input: 
promise1 = new Promise(resolve => setTimeout(() => resolve(2), 20)), 
promise2 = new Promise(resolve => setTimeout(() => resolve(5), 60))
Output: 7
Explanation: The two input promises resolve with the values of 2 and 5 respectively. The returned promise should resolve with a value of 2 + 5 = 7. The time the returned promise resolves is not judged for this problem.

Example 2:

Input: 
promise1 = new Promise(resolve => setTimeout(() => resolve(10), 50)), 
promise2 = new Promise(resolve => setTimeout(() => resolve(-12), 30))
Output: -2
Explanation: The two input promises resolve with the values of 10 and -12 respectively. The returned promise should resolve with a value of 10 + -12 = -2.

 

Constraints:

    promise1 and promise2 are promises that resolve with a number

    
<br> <hr> <br>

2723. Сложение двух промисов

Даны два промиса promise1 и promise2, верните новый промис. И promise1, и promise2 должны завершиться числом. Возвращаемый промис должен завершиться суммой этих двух чисел.

Пример 1:

Входные данные:
promise1 = new Promise(resolve => setTimeout(() => resolve(2), 20)),
promise2 = new Promise(resolve => setTimeout(() => resolve(5), 60))
Выходные данные: 7
Пояснение: Два входных промиса завершаются значениями 2 и 5 соответственно. Возвращаемый промис должен завершиться значением 2 + 5 = 7. Время завершения возвращаемого промиса в этой задаче не учитывается.

Пример 2:

Входные данные:
promise1 = new Promise(resolve => setTimeout(() => resolve(10), 50)),
promise2 = new Promise(resolve => setTimeout(() => resolve(-12), 30))
Выходные данные: -2
Пояснение: Два входных промиса разрешаются со значениями 10 и -12 соответственно. Возвращаемый промис должен разрешаться со значением 10 + -12 = -2.

Ограничения:

promise1 и promise2 — это промисы, которые разрешаются с числом.

