2770. Maximum Number of Jumps to Reach the Last Index

Medium

<p>You are given a <strong>0-indexed</strong> array <code>nums</code> of <code>n</code> integers and an integer <code>target</code>.</p>

<p>You are initially positioned at index <code>0</code>. In one step, you can jump from index <code>i</code> to any index <code>j</code> such that:</p>

<ul>
	<li><code>0 &lt;= i &lt; j &lt; n</code></li>
	<li><code>-target &lt;= nums[j] - nums[i] &lt;= target</code></li>
</ul>

<p>Return <em>the <strong>maximum number of jumps</strong> you can make to reach index</em> <code>n - 1</code>.</p>

<p>If there is no way to reach index <code>n - 1</code>, return <code>-1</code>.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<pre><strong>Input:</strong> nums = [1,3,6,4,1,2], target = 2
<strong>Output:</strong> 3
<strong>Explanation:</strong> To go from index 0 to index n - 1 with the maximum number of jumps, you can perform the following jumping sequence:
- Jump from index 0 to index 1. 
- Jump from index 1 to index 3.
- Jump from index 3 to index 5.
It can be proven that there is no other jumping sequence that goes from 0 to n - 1 with more than 3 jumps. Hence, the answer is 3. </pre>

<p><strong class="example">Example 2:</strong></p>

<pre><strong>Input:</strong> nums = [1,3,6,4,1,2], target = 3
<strong>Output:</strong> 5
<strong>Explanation:</strong> To go from index 0 to index n - 1 with the maximum number of jumps, you can perform the following jumping sequence:
- Jump from index 0 to index 1.
- Jump from index 1 to index 2.
- Jump from index 2 to index 3.
- Jump from index 3 to index 4.
- Jump from index 4 to index 5.
It can be proven that there is no other jumping sequence that goes from 0 to n - 1 with more than 5 jumps. Hence, the answer is 5. </pre>

<p><strong class="example">Example 3:</strong></p>

<pre><strong>Input:</strong> nums = [1,3,6,4,1,2], target = 0
<strong>Output:</strong> -1
<strong>Explanation:</strong> It can be proven that there is no jumping sequence that goes from 0 to n - 1. Hence, the answer is -1. 
</pre>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>2 &lt;= nums.length == n &lt;= 1000</code></li>
	<li><code>-10<sup>9</sup>&nbsp;&lt;= nums[i]&nbsp;&lt;= 10<sup>9</sup></code></li>
	<li><code>0 &lt;= target &lt;= 2 * 10<sup>9</sup></code></li>
</ul>

<br> <hr> <br>

2770. Максимальна кількість переходів для досягнення останнього індексу

Середній

<p>Вам задано масив <code>nums</code> з <code>n</code> цілих чисел з <strong>0</strong> і ціле число <code>target</code>.</p>

<p>Ви спочатку знаходитесь на індексі <code>0</code>. За один крок ви можете перейти з індексу <code>i</code> на будь-який індекс <code>j</code> таким чином, що:</p>

<ul>
<li><code>0 <= i < j < n</code></li>
<li><code>-target <= nums[j] - nums[i] <= target</code></li>
</ul>

<p>Повернути <em><strong>максимальну кількість стрибків</strong>, яку можна зробити, щоб досягти індексу</em> <code>n - 1</code>.</p>

<p>Якщо немає способу досягти індексу <code>n - 1</code>, повернути <code>-1</code>.</p>

<p>&nbsp;</p>
<p><strong class="example">Приклад 1:</strong></p>

<pre><strong>Вхід:</strong> nums = [1,3,6,4,1,2], target = 2
<strong>Вихід:</strong> 3
<strong>Пояснення:</strong> Щоб перейти від індексу 0 до індексу n - 1 з максимальною кількістю стрибків, можна виконати наступні стрибки послідовність:
- Перехід від індексу 0 до індексу 1.
- Перехід від індексу 1 до індексу 3.
- Перехід від індексу 3 до індексу 5.
Можна довести, що не існує іншої послідовності переходів, яка переходить від 0 до n - 1 з більш ніж 3 стрибками. Отже, відповідь — 3. </pre>

<p><strong class="example">Приклад 2:</strong></p>

<pre><strong>Вхід:</strong> nums = [1,3,6,4,1,2], target = 3
<strong>Вихід:</strong> 5
<strong>Пояснення:</strong> Щоб перейти від індексу 0 до індексу n - 1 з максимальною кількістю стрибків, можна виконати таку послідовність стрибків:
- Перехід від індексу 0 до індексу 1.
- Перехід від індексу 1 до індексу 2.
- Перехід від індексу 2 до індексу 3.
- Перехід від індексу 3 до індексу 4.
- Перехід від індексу 4 до індексу 5.
Можна довести, що не існує іншої послідовності стрибків, яка переходить від 0 до n - 1 з більш ніж 5 стрибками. Отже, відповідь — 5. </pre>

<p><strong class="example">Приклад 3:</strong></p>

<pre><strong>Вхід:</strong> nums = [1,3,6,4,1,2], target = 0
<strong>Вихід:</strong> -1
<strong>Пояснення:</strong> Можна довести, що не існує послідовності стрибків, яка переходить від 0 до n - 1. Отже, відповідь — -1.
</pre>

<p>&nbsp;</p>
<p><strong>Обмеження:</strong></p>

<ul>
<li><code>2 <= nums.length == n <= 1000</code></li>
<li><code>-10<sup>9</sup>&nbsp;<= nums[i]<= 10<sup>9</sup></code></li>
<li><code>0 <= target <= 2 * 10<sup>9</sup></code></li>
</ul>

<br> <hr> <br>

## Опис рішення

### Підхід: Динамічне програмування (DP)

Задача вимагає знайти **максимальну** кількість стрибків від індексу `0` до індексу `n - 1`. Стрибок з індексу `i` на індекс `j` дозволений, якщо `i < j` та `|nums[j] - nums[i]| <= target`.

### Ідея

Створюємо масив `dp`, де `dp[i]` — максимальна кількість стрибків, щоб дістатися до індексу `i`.

- **Ініціалізація**: `dp[0] = 0` (ми вже на старті), решта `dp[i] = -1` (недосяжні).
- **Перехід**: для кожного індексу `j` від `1` до `n-1` перебираємо всі попередні індекси `i` від `0` до `j-1`. Якщо `dp[i] != -1` (тобто `i` досяжний) **і** `|nums[j] - nums[i]| <= target`, то оновлюємо: `dp[j] = max(dp[j], dp[i] + 1)`.
- **Відповідь**: `dp[n - 1]`. Якщо він залишився `-1`, значить дістатися неможливо.

### Покроковий розбір (Приклад 1)

**Вхід**: `nums = [1,3,6,4,1,2]`, `target = 2`

| Крок | j | Перевірка i → j | Умова `\|nums[j]-nums[i]\| <= 2` | dp |
|------|---|-----------------|----------------------------------|----|
| 1 | 1 | 0→1: \|3-1\|=2 ✅ | dp[1] = max(-1, 0+1) = **1** | [0,1,-1,-1,-1,-1] |
| 2 | 2 | 0→2: \|6-1\|=5 ❌, 1→2: \|6-3\|=3 ❌ | dp[2] = **-1** | [0,1,-1,-1,-1,-1] |
| 3 | 3 | 0→3: \|4-1\|=3 ❌, 1→3: \|4-3\|=1 ✅ | dp[3] = max(-1, 1+1) = **2** | [0,1,-1,2,-1,-1] |
| 4 | 4 | 0→4: \|1-1\|=0 ✅, 1→4: \|1-3\|=2 ✅, 3→4: \|1-4\|=3 ❌ | dp[4] = max(-1, 0+1, 1+1) = **2** | [0,1,-1,2,2,-1] |
| 5 | 5 | 0→5: \|2-1\|=1 ✅, 1→5: \|2-3\|=1 ✅, 3→5: \|2-4\|=2 ✅, 4→5: \|2-1\|=1 ✅ | dp[5] = max(-1, 1, 2, **3**, 3) = **3** | [0,1,-1,2,2,**3**] |

**Відповідь**: `dp[5] = 3` ✅

### Складність

- **Часова**: `O(n²)` — два вкладені цикли по масиву.
- **Просторова**: `O(n)` — масив `dp` розміром `n`.

### Код рішення

```javascript
var maximumJumps = function(nums, target) {
    const n = nums.length;
    const dp = new Array(n).fill(-1);
    dp[0] = 0;

    for (let j = 1; j < n; j++) {
        for (let i = 0; i < j; i++) {
            if (dp[i] !== -1 && Math.abs(nums[j] - nums[i]) <= target) {
                dp[j] = Math.max(dp[j], dp[i] + 1);
            }
        }
    }

    return dp[n - 1];
};
```