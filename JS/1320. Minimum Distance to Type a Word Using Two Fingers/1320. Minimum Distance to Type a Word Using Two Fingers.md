1320. Minimum Distance to Type a Word Using Two Fingers
Hard

<p>You have a keyboard layout as shown above in the <strong>X-Y</strong> plane, where each English uppercase letter is located at some coordinate.</p>

<ul>
	<li>For example, the letter <code>'A'</code> is located at coordinate <code>(0, 0)</code>, the letter <code>'B'</code> is located at coordinate <code>(0, 1)</code>, the letter <code>'P'</code> is located at coordinate <code>(2, 3)</code> and the letter <code>'Z'</code> is located at coordinate <code>(4, 1)</code>.</li>
</ul>

<p>Given the string <code>word</code>, return <em>the minimum total <strong>distance</strong> to type such string using only two fingers</em>.</p>

<p>The <strong>distance</strong> between coordinates <code>(x<sub>1</sub>, y<sub>1</sub>)</code> and <code>(x<sub>2</sub>, y<sub>2</sub>)</code> is <code>|x<sub>1</sub> - x<sub>2</sub>| + |y<sub>1</sub> - y<sub>2</sub>|</code>.</p>

<p><strong>Note</strong> that the initial positions of your two fingers are considered free so do not count towards your total distance, also your two fingers do not have to start at the first letter or the first two letters.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<pre><strong>Input:</strong> word = "CAKE"
<strong>Output:</strong> 3
<strong>Explanation:</strong> Using two fingers, one optimal way to type "CAKE" is: 
Finger 1 on letter 'C' -&gt; cost = 0 
Finger 1 on letter 'A' -&gt; cost = Distance from letter 'C' to letter 'A' = 2 
Finger 2 on letter 'K' -&gt; cost = 0 
Finger 2 on letter 'E' -&gt; cost = Distance from letter 'K' to letter 'E' = 1 
Total distance = 3
</pre>

<p><strong class="example">Example 2:</strong></p>

<pre><strong>Input:</strong> word = "HAPPY"
<strong>Output:</strong> 6
<strong>Explanation:</strong> Using two fingers, one optimal way to type "HAPPY" is:
Finger 1 on letter 'H' -&gt; cost = 0
Finger 1 on letter 'A' -&gt; cost = Distance from letter 'H' to letter 'A' = 2
Finger 2 on letter 'P' -&gt; cost = 0
Finger 2 on letter 'P' -&gt; cost = Distance from letter 'P' to letter 'P' = 0
Finger 1 on letter 'Y' -&gt; cost = Distance from letter 'A' to letter 'Y' = 4
Total distance = 6
</pre>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>2 &lt;= word.length &lt;= 300</code></li>
	<li><code>word</code> consists of uppercase English letters.</li>
</ul>

<br> <hr> <br>

1320. Минимальное расстояние для набора слова двумя пальцами
Сложно

<p>У вас есть раскладка клавиатуры, показанная выше в плоскости <strong>X-Y</strong>, где каждая английская заглавная буква расположена в некоторой координате.</p>

<ul>

<li>Например, буква <code>'A'</code> расположена в координате <code>(0, 0)</code>, буква <code>'B'</code> расположена в координате <code>(0, 1)</code>, буква <code>'P'</code> расположена в координате <code>(2, 3)</code>, а буква <code>'Z'</code> расположена в координате <code>(4, 1)</code>.</li>
</ul>

<p>Дана строка <code>word</code>, верните <em>минимальное общее <strong>расстояние</strong> для набора этой строки только двумя пальцами</em>.</p>

<p>The Расстояние между координатами (x₁, y₁) и (x₂, y₂) равно |x₁ - x₂| + |y<sub>1</sub> - y<sub>2</sub>|</code>.</p>

<p><strong>Обратите внимание</strong>, что начальные положения ваших двух пальцев считаются свободными и не учитываются в общем расстоянии, а также ваши два пальца не обязательно должны начинаться с первой буквы или первых двух букв.</p>

<p>&nbsp;</p>
<p><strong class="example">Пример 1:</strong></p>

<pre><strong>Ввод:</strong> слово = "CAKE"
<strong>Вывод:</strong> 3
<strong>Пояснение:</strong> Используя два пальца, один из оптимальных способов набрать "CAKE" — это:
Палец 1 на букве 'C' > стоимость = 0
Палец 1 на букве 'A' > Стоимость = Расстояние от буквы «C» до буквы «A» = 2
Палец 2 на букве «K» > стоимость = 0
Палец 2 на букве «E» > стоимость = Расстояние от буквы «K» до буквы «E» = 1
Общее расстояние = 3
</pre>

<p><strong class="example">Пример 2:</strong></p>

<pre><strong>Ввод:</strong> слово = "HAPPY"
<strong>Вывод:</strong> 6
<strong>Пояснение:</strong> Используя два пальца, оптимальный способ набрать "HAPPY" — это:
Палец 1 на букве «H» > стоимость = 0
Палец 1 на букве «A» > стоимость = Расстояние от буквы «H» до буквы «A» = 2
Палец 2 на букве «P» > стоимость = 0
Палец 2 на букве 'P' > стоимость = Расстояние от буквы 'P' до буквы 'P' = 0
Палец 1 на букве 'Y' > стоимость = Расстояние от буквы 'A' до буквы 'Y' = 4
Общее расстояние = 6
</pre>

<p></p>
<p><strong>Ограничения:</strong></p>

<ul>

<li><code>2 <= длина слова <= 300</code></li>

<li><code>слово</code> состоит из заглавных английских букв.</li>
</ul>

<br> <hr> <br>

Для розв’язання цієї задачі найефективнішим підходом є **динамічне програмування (DP)** з оптимізацією стану. Оскільки при кожному кроці (натисканні літери) один із двох пальців обов’язково опиняється на поточній літері, нам достатньо відстежувати лише позицію **іншого** пальця.

### Основна логіка

1.  **Координати:** Клавіатура має 6 колонок. Код літери (0–25) перетворюється на координати $(x, y)$ за формулами:
    * $row = \lfloor code / 6 \rfloor$
    * $col = code \pmod 6$
2.  **Відстань:** Використовується Мангеттенська відстань: $$|x_1 - x_2| + |y_1 - y_2|$$
3.  **Стан DP:** `dp(index, other_finger_pos)` — мінімальна відстань для завершення слова, починаючи з символу за номером `index`, якщо один палець уже стоїть на символі `word[index-1]`, а інший — на позиції `other_finger_pos`.
4.  **Початкові умови:** Перше натискання будь-яким пальцем має нульову вартість. Тому ми можемо вважати, що перший палець автоматично стає на `word[0]`, а другий залишається "вільним" (`null`).

---

### Реалізація (JavaScript)

```javascript
/**
 * @param {string} word
 * @return {number}
 */
var minimumDistance = function(word) {
    const n = word.length;
    // Мемоїзація: [індекс поточного символу][позиція іншого пальця (0-25 або 26 для null)]
    const memo = Array.from({ length: n }, () => new Array(27).fill(-1));

    // Функція розрахунку координат
    const getCoords = (charIdx) => [Math.floor(charIdx / 6), charIdx % 6];

    // Функція розрахунку відстані між двома символами (літерами)
    const getDist = (c1, c2) => {
        if (c1 === null || c2 === null) return 0;
        const code1 = c1.charCodeAt(0) - 65;
        const code2 = c2.charCodeAt(0) - 65;
        const [r1, l1] = getCoords(code1);
        const [r2, l2] = getCoords(code2);
        return Math.abs(r1 - r2) + Math.abs(l1 - l2);
    };

    const solve = (idx, otherChar) => {
        if (idx === n) return 0;

        // Перетворюємо символ іншого пальця в індекс для мемоїзації
        const otherIdx = otherChar === null ? 26 : otherChar.charCodeAt(0) - 65;
        if (memo[idx][otherIdx] !== -1) return memo[idx][otherIdx];

        const prevChar = word[idx - 1];
        const currentChar = word[idx];

        // Варіант 1: Рухаємо палець, який щойно натиснув попередню літеру (word[idx-1])
        const moveFirst = getDist(prevChar, currentChar) + solve(idx + 1, otherChar);

        // Варіант 2: Рухаємо "інший" палець до поточної літери
        const moveSecond = getDist(otherChar, currentChar) + solve(idx + 1, prevChar);

        return memo[idx][otherIdx] = Math.min(moveFirst, moveSecond);
    };

    // Починаємо з індексу 1, бо перший палець уже на word[0] (ціна 0)
    return solve(1, null);
};
```

---

### Аналіз складності

* **Часова складність:** $O(N \times \Sigma)$, де $N$ — довжина слова (до 300), а $\Sigma$ — розмір алфавіту (26). Це приблизно $300 \times 26 = 7800$ ітерацій, що є надзвичайно швидким для JS-рушія.
* **Просторова складність:** $O(N \times \Sigma)$ для зберігання таблиці мемоїзації.

### Чому це ефективно?
Використання рекурсії з мемоїзацією дозволяє уникнути повторних обчислень тих самих станів. Замість того, щоб перебирати всі можливі комбінації рухів двох пальців (що дало б експоненційну складність $2^N$), ми розглядаємо лише обмежену кількість логічних станів.